[pytest]
testpaths = tests

# Temporary directories of the tests stay inside the repository instead of the
# shared Windows temp. There pytest keeps a "pytest-current" symlink it cannot
# always remove afterwards, and the failed sweep crashes the session AFTER every
# test has already passed - which reads as a broken suite when nothing is broken.
addopts = --basetemp=.pytest_tmp
