# GENERATED by scripts/make_configs.py from configs/frozen.yaml.
# Edit the decision there, not this file: the next run of the generator
# overwrites whatever is written here.
#
# The full pipeline without motion: the other half of that signal's marginal contribution.

experiment: full_no_motion
dataset: vatex
seed: 1234
frames:
  step_s: 1.25
  dedup_cosine: 0.9
components:
  scene_embedding:
    enabled: true
    model: openclip_vit_h14
  caption:
    enabled: true
    model: llava_1_5_7b
  objects:
    enabled: true
    detector: yolo11
  face_regions:
    enabled: false
  identity:
    enabled: false
  motion:
    enabled: false
scoring:
  scope: full_collection
  normalization: zscore
  weights:
    mode: uniform
evaluation:
  report_by:
  - requirements
  - complexity
  - kinetics_vocab
matching:
  measure: top1
  threshold: 0.8054
  threshold_by_vocab:
    objects_yolo11: 0.8054
    objects_yoloe_promptfree: 0.733
    expressions_hsemotion: 0.8048
    actions_kinetics400: 0.8017
