# GENERATED by scripts/make_configs.py from configs/frozen.yaml.
# Edit the decision there, not this file: the next run of the generator
# overwrites whatever is written here.
#
# E2-A: the base representation alone.

experiment: E2-A
dataset: vatex
seed: 1234
frames:
  step_s: 1.25
  dedup_cosine: 0.9
components:
  scene_embedding:
    enabled: true
    model: openclip_vit_h14
  caption:
    enabled: false
  objects:
    enabled: false
  face_regions:
    enabled: false
  identity:
    enabled: false
  motion:
    enabled: false
scoring:
  scope: full_collection
  normalization: zscore
  weights:
    mode: uniform
evaluation:
  report_by:
  - requirements
  - complexity
  - kinetics_vocab
