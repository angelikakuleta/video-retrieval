# GENERATED by scripts/make_configs.py from configs/frozen.yaml.
# Edit the decision there, not this file: the next run of the generator
# overwrites whatever is written here.
#
# E4-D: the base re-ranked by a query-time detector.

experiment: E4-D
dataset: office
seed: 1234
segmentation:
  strategy: shots_histogram
  min_len_s: 3.0
  max_len_s: 15.0
  window_s: 10.0
  histogram_threshold: 0.5
frames:
  step_s: 1.25
  dedup_cosine: 0.9
components:
  scene_embedding:
    enabled: true
    model: openclip_vit_h14
  caption:
    enabled: false
  objects:
    enabled: false
  face_regions:
    enabled: false
  identity:
    enabled: false
  motion:
    enabled: false
scoring:
  scope: full_collection
  normalization: zscore
  weights:
    mode: uniform
  query_time_detection:
    enabled: true
    detector: yoloe_prompted
    candidates_top_n: 50
evaluation:
  report_by:
  - requirements
  - complexity
